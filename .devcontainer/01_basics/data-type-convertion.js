let matchScore = "100";

let currentScore = Number(typeof(matchScore));
console.log(currentScore);

// conversion to boolean and remaining in same way
let Score = "100";

let curScore = Boolean(typeof(Score));
console.log(curScore);